
void selectionSort(){

  int arr[5] = {7,1,4,3,9};
  
  for(int i = 0 ; i < 4 ; i++){
    
    int minIndex = i; 

     for(int j = i + 1 ; j < 4 ; j++){

        if(arr[j] < minIndex)
           minIndex = j;
                
     }
    swap(arr[minIndex],arr[i]);   
  }
}
